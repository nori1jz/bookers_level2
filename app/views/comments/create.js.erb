$("#comments-<%= @book.id %> .comments-count").html("コメント数: <%= @book.comments.count %>")
$(".comment").html("<%= escape_javascript(render partial: 'comments/comment', locals: {book: @book}) %>");
$("textarea").val('')