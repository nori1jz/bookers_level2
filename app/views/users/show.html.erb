<p id="notice"><%= flash[:notice] %></p>

</tbody>
</table>
<h2>User info</h2>
  <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>

<p>name <%= @user.name %></p>
<p>introdaction</p>

<h2>New book</h2>
<% if @post_book.errors.any? %>
  <%= @post_book.errors.count %>error prohibited this book from being saved:
  <% @post_book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
 <% end %>
<!-- form_with部分 -->
<%= form_with model:@post_book, local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %>

<br><%= f.submit 'Create Book' %></br>

<% end %>

<h1>Books</h1>

<table>
<thead>
<tr>
<th>Title</th>
<th>Opinion</th>
<th colspan="3"></th>
</tr>
</thead>

<tbody>
<% @post_books.each do |post_book| %>
<tr>
<td>
<%= post_book.title %>
</td>
<td>
<%= post_book.body %>
</td>
<td>
</td>
<td>
<%= link_to "Show", post_books_path(current_user) %>
<%= link_to "Destroy", post_books_path(current_user), method: :delete, "data-confirm" => "Are you sure?" %>

</td>
</tr>
<% end %>